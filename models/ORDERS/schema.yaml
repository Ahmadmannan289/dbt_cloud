version: 2

models:
    - name: orders
      description: "This is dbt transformation for order table"
      columns:
          - name: order_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null